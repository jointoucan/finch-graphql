query getMessages {
  _finchDevtools {
    enabled
    messages {
      operationName
      rawQuery
      variables
      initializedAt
      timeTaken
      response
      context
    }
  }
}

mutation enableMessages($enabled: Boolean) {
  _enableFinchDevtools(enabled: $enabled)
}
